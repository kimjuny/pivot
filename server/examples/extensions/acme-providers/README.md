# ACME Providers

Sample extension package for local import development in Pivot.

This package intentionally stays small:

- One channel provider with key `acme@chat`
- One web-search provider with key `acme@search`

You can import this folder directly from the Extensions page to validate the
local preview, trust, install, and binding flow.
